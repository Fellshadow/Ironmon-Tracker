name: Luacheck
on:
  push:
    branches-ignore: ['fellshadow/main']
jobs:
  sile:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Luacheck linter
        uses: lunarmodules/luacheck@v1.0.0
        with:
          args: '--std lua51c'
